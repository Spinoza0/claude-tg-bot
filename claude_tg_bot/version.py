"""Bot version — a single source for package runs and script runs.

Kept separate from config.py so a release version can be bumped without
touching the configuration.
"""

BOT_VERSION: str = "0.7.5"
